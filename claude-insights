#!/bin/bash
# Extract work insights from Claude JSONL sessions

TIME_TRACKER_DIR="/home/rolf/fuzzy-ops/tools/time-tracker-integration"

# Change to time-tracker directory and run with uv
cd "$TIME_TRACKER_DIR" && uv run python extract_claude_work_insights.py "$@"