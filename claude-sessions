#!/bin/bash
# Find Claude sessions with gap detection and duration calculation

TIME_TRACKER_DIR="/home/rolf/fuzzy-ops/tools/time-tracker-integration"

# Change to time-tracker directory and run with uv
cd "$TIME_TRACKER_DIR" && uv run python find_claude_sessions_enhanced.py "$@"