#!/bin/bash
# Time tracker wrapper for analyzing coding sessions across tools

TIME_TRACKER_DIR="/home/rolf/fuzzy-ops/tools/time-tracker-integration"

# Change to time-tracker directory and run with uv
cd "$TIME_TRACKER_DIR" && uv run src/time_tracker.py "$@"